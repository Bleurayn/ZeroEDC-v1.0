version: "3.9"
services:
  zeroedc:
    build: .
    ports:
      - "8080:8080"
      - "7860:7860"
    volumes:
      - .:/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      retries: 3
